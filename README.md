# Flux demo

This repository contains a simple Flux GitOps setup for a local Kubernetes cluster.

It manages:
- a `whoami` sample app
- Prometheus and Loki via Helm releases
- Grafana via a dedicated Helm release
